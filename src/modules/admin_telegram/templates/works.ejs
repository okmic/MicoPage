*Как это работает:*
<% if (works.length > 0) { %>
    <% works.forEach(work => { %>
        - *Название:* <%= work.title %>
          *Изображение:* <%= work.imgUrl %>
    <% }) %>
<% } else { %>
    Нет доступных работ.
<% } %>